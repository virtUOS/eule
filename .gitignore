# Python (gateway)
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage/

# Node (widget)
node_modules/
dist/
test-results/
playwright-report/
.playwright/

# Env / secrets (config references *_env names; values never committed)
.env
.env.*
# the template carries only var NAMES + placeholders — safe to commit
!.env.example

# OS / editor
.DS_Store
*.swp

# Claude Code local scratch (jobs, worktrees, scheduled-task locks)
.claude/
